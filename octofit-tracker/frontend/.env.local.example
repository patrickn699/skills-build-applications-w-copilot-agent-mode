## Example env file for Codespaces
# Set this when running the frontend inside a GitHub Codespace so the app can reach the backend.
# Replace with your actual Codespace name.
VITE_CODESPACE_NAME=your-codespace-name
